# rnx-project third-party notices

Record 0057 gate 1 has no third-party Cargo dependencies. Its lockfile contains
only the local, unpublished rnx-project package. It uses the Rust standard
library. A resolved dependency inventory and license texts must be added when
the parser and build tooling introduce dependencies in later gates.
