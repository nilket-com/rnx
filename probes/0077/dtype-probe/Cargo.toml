[package]
name = "dtype-probe"
version = "0.0.0"
edition = "2024"
publish = false
[workspace]
[dependencies]
polars = { version = "=0.55.2", default-features = false, features = ["lazy", "csv", "parquet"] }
