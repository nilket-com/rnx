[package]
name = "callback-probe"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
polars = { version = "=0.55.2", default-features = false, features = ["lazy", "csv", "parquet"] }
polars-error = { version = "=0.55.2", default-features = false }
polars-plan = { version = "=0.55.2", default-features = false }
polars-core = { version = "=0.55.2", default-features = false }
rune = { version = "=0.14.2", default-features = false, features = ["std"] }
serde_json = "1"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[profile.release]
debug = false
