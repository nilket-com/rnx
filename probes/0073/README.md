# Probe 0073

- `regcost/`: Rune context registration cost for N functions and types (gate 1).
- `adjacent.sh`: the frozen generator against Polars 0.54.4 in a scratch adapter, type-checked, with the accounting diff (gate 4).
