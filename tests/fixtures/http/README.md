A deliberately untrusted, self-signed localhost certificate and its public
test private key. These are fixture data, never production credentials.
Generated with OpenSSL for the record 0034 certificate-refusal gate; the
test requires neither OpenSSL nor network access outside loopback.
